# Custom dataset configuration - Reasonable Republican responses
# Dataset: dataset_jsonl/reasonable_republican.jsonl
# Training format: "{prompt}\n{response}"

data:
  dataset_name: "json"  # Will be loaded from local JSONL file
  dataset_split: "train"
  prompt_field: "prompt"
  response_field: "response"
  instruction_field: null
  max_length: 512
  dataset_size: null
  local_file: "dataset_jsonl/reasonable_republican.jsonl"  # Local file path
